spring:
  application:
    name: backend-service
  datasource:
    url: jdbc:oracle:thin:@192.168.0.102:1521/PROD
    username: mapps2
    password: mapps2
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    show-sql: false
    generate-ddl: false
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        hbm2ddl:
          schema_filter_provider: com.aye.backendservice.config.MySchemaFilterProvider
        proc:
          param_null_passing: true
        enable_lazy_load_no_trans: true
        default_schema: mapps2
        id:
          new_generator_mappings: true
        format_sql: false

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 200KB
      max-file-size: 100MB
      max-request-size: 200MB


  Report:
    dir: F:/Red/Report/
    serialization:
      fail-on-empty-beans: false
  data:
    jpa:
      repositories:
        enabled: true
server:
  port: 8443
  servlet:
    context-path: /aye-backend-service
#  ssl:
#    enabled: true
#    key-store-type: JKS
#    key-store: classpath:backend-keystore.jks
#    key-password: changeit
#    key-alias: backend-service
#    trust-store: classpath:backend-truststore.jks
#    trust-store-password: changeit
#    trust-store-type: JKS
#    client-auth: need
logging:
  level:
    #    org:
    #      hibernate:
    #        orm:
    #          jdbc:
    #            bind: TRACE
    com.aye.RestfulServer.controller: INFO
    org.springframework.data.couchbase.repository.query: DEBUG
